services:
  api:
    build:
      context: .
      dockerfile: server/Dockerfile
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - server/.env.production
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/server/data/prod.db
      - UPLOAD_DIR=/app/server/uploads
    volumes:
      - ./deploy-data/db:/app/server/data
      - ./deploy-data/uploads:/app/server/uploads
